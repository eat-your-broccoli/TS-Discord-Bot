/**
 * models command configuration
 */
export default class CommandConfig {
  // command can only be executed in bot channel
  public botChannelOnly = false;

  // command can only be performed by owner
  public ownerOnly = false;
}
